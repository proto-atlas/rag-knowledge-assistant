# スクリーンリーダー手動アクセシビリティ確認記録

## 日本語要約

この記録は、公開URLの主要導線をWindows Narrator + Chromeで手動確認した特定時点ログです。

- 確認したこと: トップページ、確認用キー入力、質問フォーム、ストリーミング表示と状態表示、引用番号、根拠カードへのフォーカス移動、エラー表示の7状態
- 結果: 作成者によるWindows Narrator手動確認では、7状態すべてOK、気になった点なし
- 読み方: axe-core自動検査とは別に、主要導線がスクリーンリーダーで追えるかを手動で確認した補助記録です
- この記録に含めない範囲: 完全なWCAG監査、NVDA / VoiceOver / JAWSを含む製品別比較、全ブラウザ・全UI状態の網羅

補助確認として、公開URLに対するPlaywrightチェックで、ラベル、主要ボタン、モックSSE表示、引用番号ボタン、根拠カードへのフォーカス移動、エラー表示のDOM導線も確認しています。API経路はモック化し、実確認用キー値や実プロバイダーは使っていません。

生成日時: 2026-05-03T15:45:10+09:00
手動確認日時: 2026-05-03T15:40:00+09:00
確認種別: スクリーンリーダー手動確認
結果: 通過（制約あり）

## 手動確認環境

| 項目 | 値 |
|---|---|
| OS | Windows 11 |
| ブラウザ | Chrome |
| スクリーンリーダー | Windows Narrator |
| URL | public URL |
| 対象 | モック応答を使った公開UI |

## 手動確認結果

| 状態 | 結果 | メモ |
|---|---|---|
| トップページ | OK | 気になった点なし |
| 確認用キー | OK | 気になった点なし |
| 質問フォーム | OK | 気になった点なし |
| ストリーミング表示と状態表示 | OK | 気になった点なし |
| 引用番号 | OK | 気になった点なし |
| 根拠カードへのフォーカス移動 | OK | 気になった点なし |
| エラー表示 | OK | 気になった点なし |

## 補足Playwright確認

補足確認では、公開URLに対してヘッドレスChromiumセッションを使いました。ブラウザセッション内で `/api/ask` と `/api/search` の応答をモック化し、実確認用キーは送信していません。

| 確認項目 | 結果 | 詳細 |
|---|---|---|
| タイトル | 通過 | `RAG Knowledge Assistant — 架空文書RAG確認画面` |
| 確認用キーのラベル | 通過 | ラベル付き入力欄を1件確認 |
| 質問欄のラベル | 通過 | ラベル付き入力欄を1件確認 |
| 検索ボタン | 通過 | `根拠候補だけ検索` を確認 |
| 回答生成ボタン | 通過 | `検索して回答生成` を確認 |
| モックSSE回答の表示 | 通過 | SSEのモック応答を画面に表示 |
| 引用番号ボタン `[1]` | 通過 | 引用番号がボタンとして表示されることを確認 |
| 引用番号操作後の根拠カードフォーカス | 通過 | `[1]` を押すと対応する根拠カード本文へフォーカスが移動 |
| エラー表示 | 通過 | `確認用キーが必要です。` がalert内に表示されることを確認 |

## 既存の検証記録との関係

- `a11y-review-2026-05-02.md` は、ソース上のアクセシビリティ実装確認を記録しています。
- `axe-a11y-2026-05-02.md` は、ローカルのモック応答UI 4状態に対するaxe-core自動検査を記録しています。
- メインの公開UI経路に対するWindows Narrator手動確認を追加した記録です。

## この記録に含めない範囲

- これはWCAG 2.1 AAまたはWCAG 2.2 AA全体の監査ではありません。
- これはスクリーンリーダー製品ごとの比較ではありません。
- NVDA、VoiceOver、JAWS、TalkBack、ブラウザ別の網羅確認は主張しません。
- 今後追加されるすべてのUI状態がアクセシブルであるとは主張しません。
- Claude実API、Workers AI、Vectorize、D1、実プロバイダー経路は呼び出しません。
- 補足のPlaywright確認では実確認用キーを送信していません。
